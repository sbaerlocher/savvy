---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: savvy-development

namespace: savvy-dev

# namePrefix: dev-  # REMOVED: Breaks selectors (Service → Deployment)
# Isolation via namespace: savvy-dev

resources:
  - ../../base
  - ingress.yaml
  - postgres.yaml

patches:
  - path: deployment-patch.yaml
  - path: configmap-patch.yaml

images:
  - name: ghcr.io/sbaerlocher/container/savvy
    newTag: dev

replicas:
  - name: savvy
    count: 1

labels:
  - pairs:
      environment: development
